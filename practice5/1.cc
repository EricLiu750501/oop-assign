#include <iostream>

using namespace std;

class Student {
    public:
        Student(int c1, int c2);
        avg();
        sort();
    
    private:
        int stus;
        Subject mysub;
        
        
}

Student::Student(int c1, int c2) : {
    this->stus = c1;

}

class Subject {
    public:

        Subject(int c2);
        avg();
        sort();
    private:
        int *subs;


}

int main() {
    Student S;
    int n;
    int c1, c2;

    cin >> n;

    for (int i=0;i<n;i++) {
        
        cin >> c1 >> c2;
        S()


    }


}